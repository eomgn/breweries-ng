import { BreweryTypeDirective } from './brewery-type.directive';

describe('BreweryTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new BreweryTypeDirective();
    expect(directive).toBeTruthy();
  });
});
